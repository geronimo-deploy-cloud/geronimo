"""Geronimo CLI Module.

The command-line interface (CLI) serves as the primary entry point for interacting
with the Geronimo platform. It provides commands for:
- Initializing new projects (`geronimo init`)
- Managing configurations (`geronimo config`)
- Deploying infrastructure (`geronimo deploy`)
- Managing API keys for Service to Service authentication (`geronimo keys`)
- Managing authentication to Geronimo Cloud (`geronimo auth`)

This module contains the Click-based command definitions and the main entry point logic.
"""
